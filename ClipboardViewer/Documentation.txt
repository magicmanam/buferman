- file encryption
- console has the program much slowlier
- Read WinForms book!
- Complete presentation!
- Log4net mastering!
- Git!
- Increase height
- unknown publisher error
- Window with hot keys + menu with applications

//Refactor: add DI
//Improve Performance (try different tools)

---- New branch ----
//Save the most popular
//Basket for deleted bufers (to quickly restore)

Settings:  //Minimalize settings to ease using
  - Bufer's width: 300 by default
  - Show tooltip when bufer gets focus (arrows keys or Tab). Set up time of tooltip
  - Show tooltip when mouse is over bufer
  - Show confirmation dialog on delete
  - Max bufers count: *16, 30
  - Show bufers in *one column, *two columns, *three columns
?  - History: store current day bufers, only from program start, when amount does not preceeds 100Mb of RAM
?  - Close app on Escape key (Esc) - это настройка ради настройки, никакого удобства она не несёт, лучше сделать другую прилагу, с полезной нагрузкой




	Документация:
Чтобы отменить удаление/добавление буфера, используйте горячие клавиши Ctrl + Z
Сразу же при запуске программы в буфере будет находится текущий клип (если он там есть).
Программа поддерживает русские и английские буферы.
Текст кнопки буфера показывает текст буфера с обрезанными лидирующими и заканчивающими знаками невидимых символов (пробелы, перевод каретки и т.д.).
Программа сохраняет буферы разных типов: любые тексты (plain text, html, Windows Office), файлы, снимки экрана и т.д.
Если при копировании в буфер приложение крешиться, опишите в каком приложении вы копируете и напишите на почту...
Для каждого буфера хранятся все его форматы, в которых он представлен. Форматы доступны через контекстное меню (правой кнопкой мыши).
Если буфер содержит картинку, то превью картинки можно увидеть в тултипе соответствующего буфера.
Программу желательно запускать в режиме Администратора, для корректной работы с приложениями, уже запущенными в этом режиме.
При выборе буфера, он поднимается вверх списка буферов, так как попадает в него снова.
Приложение позволяет сохранять буферы-файлы. Для удобства такие буферы обозначаются коричневым цветом.
В один момент времени на одном компьютере может быть запущено не более одного экземпляра программы. В панели задач пиктограмма программы отсутствует.
По нажатию на клавишу Escape, программа сворачивается.
Список доступных буферов появляется по нажатию на клавиши Alt+C. Навигироваться по буферам можно стрелками управления курсором, или клавишей Tab.
Для выбора нужного буфера, нужно нажать клавишу Enter или нажать левой кнопкой мыши. Буфер вставится туда, где стоит курсор активного окна.
Для удаления выделенного буфера нажмите кнопку Delete, или воспользуйтесь контекстным меню буфера.
При навигации по буферам клавишами управления курсора (или Tab), всплывает стандартный тултип.
После нажатия на Alt+C (или Alt+Tab), окно появляется снова, кнопки отрисовываются шириной, равной ширине окна программы.
При нажатии на кнопку закрытия окна программы или горячих клавиш Alt+F4 окно сворачивается, но не закрывается. Для закрытия программы используйте пункт меню Actions->Exit Session.
Возможность редактирования буферов отсутствует, ибо нефик. Каждый буфер хранится в нескольких форматах, в том числе бинарных, неподдающихся изменению.
Если вам нужно скрыть скопированный пароль, либо дать более смысловое имя-алиас для буфера, воспользуйтесь пунктом меню "Change text". Если изменённый таким образом буфер будет отличаться от первоначального значеня, то будет выделен жирным шрифтом. Когда текст возвращается к первоначальному, шрифт становится обычным.
Текст нельзя изменять для буферов с файлами или скриншотами.
Программа позволяет сохранять постоянные буферы (через пункт контекстного меню "Mark as persistent"), они не будут перезаписываться и всегда будут находиться внизу списка.
По нажатию на 'x' выделится первый буфер, а по нажатию на 'v' - последний.

/*Check: Все пункты меню работают. */
/*Check: Files: /n FolderPath /n FileName0, FileName1
/*Check: Пометить буфер постоянным, пункт меню должен задизэйблиться, и он переместиться вниз. При добавлении новых буферов все постоянны буферы остаются, не удаляются.*/
/*Check: Окно нельзя ресайзить (не перерисовываются кнопки). Попробовать замаксимайзить окно, растянуть - оно не должно поддаваться. */
/*Check: Когда окно программы видно на экране и в это время буфер обновляется, список буферов в окне программы обновляется сразу же. */
/*Check: Запустить студию в режиме админа и попробовать вставить туда текст. */
/*Check: Если копируется несколько файлов, то на бутоне и тултипе будет "<< Files (9) >>: firstFileName ...". Если 1 файл - "<< File >>: firstFileName". */
/*Check: При добавлении нового буфера, тултип на первом буфере не должен всплывать (окно программы должно быть открыто) */
/*Check: Выбрать буфер, нажав на соответствующую кнопку. Окно программы скрывается. */
/*Check: Скопировать несколько клипов. Открыть окно программы слева. Сделать активным другое окно справа (так, чтобы окно программы всё ещё было видимо,
но уже неактивно). Нажать Alt+C. Программа становится активной, без мерцания от перерисовки. */
/*Check: Накопировать максимум буферов. При копировании следующего буфер, он должен добавляться вверх, а самый первый должен удаляться. */
/*Check: Сделать 30 раз принтскрин (чтобы быстро наполнить буфер). Проверить, что все буферы отображаются симметрично, т.е. высота и ширина окна программы выбраны правильно. */
/*Check: Кнопка удалить всё работает */
/*Check: Запустить виндовс, и сразу прилагу, без копирования чего-либо в буфер. ничего не должно быть скопировано. */
/*Check: При изменении текста кнопки буфера, тултипы GotFocus- и MouseOver- тоже изменяются */
/*Check: Изменить второй клип. Затем вставить его кудато. Он должен оказаться вверху, с тултипами и текстом измененными. */
/*Check: Открыть файл, занятый другим приложением. Диалог с ошибкой должен показаться. */