Always keep documentation and check list up to date!!!
Done section is deleted with updating documentation and check list

******************* sorted ****************
Done - 
By DD/01/2018
***

# Release by 1 April 2018
- relocate tooltips to presentation classes and extract base class
- Fix issue with unicode/not unicode symbols in button's title
- Keyboard Emulator: ^ as : in russian keyboard

# Ultimate version (almost sorted)
- If link in bufer - open in browser if Shift pressed (or other key)
- Save deleted bufers in memory
- все атомарные операции сервиса обернуть в undoablecontext вложенные
- многострочные буферы из файла + пароль + персистентность + alias - json format
- добавить проверку буфера в файле и метки того, что буфер был взят из файла
- Window with hot keys + menu with applications (scripts tool and others)
- Подчистить проект: анализаторы, ссылки и т.д.
- Complete presentation!
- Разобраться с лицензиями
- On scroll to show current clipboard with possibility to click
- MAX_STATUS_LENGTH = 55 as well as other magic numbers/strings
- Refactor: add DI
- Set WindowLevelContext at program level, not form level ?
- Enable/disable 'Delete All' and 'Delete All Temporary' based on availability.
- Add clips count into status bar (separate strip)
- Read WinForms book!
- Add drag an drop for temp/persistent bufers
- Add plugin support
- Hosting at https://sourceforge.net and other market places.
- Add menu with unknown file formats
- If link is copied in browser, Enter should be clicked automatically.
- На основе фич сделать список подсказок и показывать их на стартапе. Чаще показывать фичи, которые редко используются
- Corporative & personal versions with private message.
- Site with users -> from menu by unique link
- Add some AI strategies based on user behaviour (Ctrl+C - TAB - Ctrl+V - TAB - Ctrl+C)
- IStorage - to remove all System.IO - to use everywhere
- Make plugins for preview tooltips with settings
- Improve Performance (try different tools)
******************* unsorted ****************
Alt + C can be overwritten in some programs (like Notepad++)
Lingvo: Ctrl-C + C
Alt-C + P - Opens Paint + other shortcuts
FtpPrivateData - ftp format - other format support can added via plugins
Remove all MessageBox.Show from libs
If program in classic mode and you foreground window is admin - show warning smwhere. If try to paste from user mode into admin mode - show alert window with reason and solution.
Show status line on every action
Automatic tests: create python git repo
There is a new bufers.txt file on every program update.
Add program to autostart (only for portable version. There is a well-known problem with ClickOnce applications: it will have several programs at once)

//MessageBox.Show("Feature is not supported now. Pay money to support.", "Keep calm and copy&paste!")
//MessageBox.Show("Available only in Free Pro version.", "Just copy&paste")

Settings:  //Minimalize settings for easy using
  - Bufer's width: 300 by default
  - Toolip duration (2500 by default)
  - Show tooltip when bufer gets focus (arrows keys or Tab). Set up time of tooltip
  - Show tooltip when mouse is over bufer
  - Show confirmation dialog on delete
  - Max bufers count: *16, 30
  - Show bufers in *one column, *two columns, *three columns
  - Show preview image instead of ordinal text
  - Change language (EN, RU)
?  - History: store current day bufers, only from program start, when amount does not preceeds 100Mb of RAM

*** Checked ***
/*Check: . */
/*Check: Load bufers file with multi languages words - all words should be loaded correctly. */
/*Check: Make screenshot. Make sure after 23 seconds new screenshot is not added. */
/*Check: Help -> Documentation - browser with page is opened. */
/*Check: Изменить буфер текст. Фокус-тултип сразу же поменялся. */
/*Check: Программу можно закрыть из контекстного меню иконки в трее. */
/*Check: Скопировать 100 и 1000 раз. Нотификация в трее появится. */
/*Check: При закрытии программы пиктограмм из трея исчезает. */
/*Check: Если программа запущена, то показывается алерт с заголовком BuferMAN (берется из Product name сборки). */
/*Check: Update bufer alias. After that check tooltip on focus and on mouse over - it should be a new alias. */
/*Check: When there is no bufers, manual in light gray color is shown. */
/*Check: Paste or load > 30 bufers. All bufers and persistent divider is visible. */
/*Check: > 7 буферов в программе. При удалении первого, фокус остаётся на первом, при удалении 2-ого - на 2-ом. При удалении последнего - на последнем и с табуляцией с конца. Тоже самое для предпоследнего. */
/*Check: В программе 2 текстовых буфера. Вставить в любое место второй буфер (они поменяются местами). А потом вставить в любое место второй буфер (тоже 2-о по счёту будет). После этого горячие клавиши X и V продолжат работать. */
/*Check: Поставить русскую локаль и скопировать файл/файлы/скрин. << Text >> should russian. */
/*Check: Для всех пунктов меню работают горячие клавиши. */
/*Check: Создать больше 2-ух постоянных буферов - они должны отображаться все. */
/*Check: Если все буферы постоянные, то при попытке копирования показывается диалог, но список не обновляется пока не удалишь буфер. */
/*Check: Несколько временных и несколько постоянных клипов. Таб на последнем временном. По нажатию на TAB фокус переходит на первый постоянный. По нажатию на V - на последнем персистентном. */
/*Check: После запуска программы сделать 3 клипа. После этого 3 раза CtrlZ. Больше CtrlZ не доступна. Сделать CtrlY - оба действия доступны. Если сделать CtrlY ещё 2 раза, то и он становится недоступным. */
/*Check: Если при запуске приложения в файле есть русские слова, то они не показываются абракадаброй. */
/*Check: При запуске приложения пункты меню Undo/Redo недоступны (если ничего не добавилось из памяти или из буфера). */
/*Check: Если есть постоянные буферы, то при удалении всех буферов, конфирмейшен всплывает. */
/*Check: При копировании нескольких пробелов, буфер показывает сколько пробелов и вставляет их в правильном кол-ве. */
/*Check: Проверить, что текстовый буфер может копировать посимвольно в консоли! Комбинации горячих клавиш здесь нет, т.к. есть проблемы с вводом! */
/*Check: Создать файл bufers.txt с двумя строками. При загрузке программы, все буферы копируются в программу (особенно обратить внимание на первый)! */
/*Check: Создать креды с спецсимволами +^%{}[] (логин и пароль) - проверить, что пароль и логин по отдельности вставляются правильно (спецсимволы заменяются на '{special_char}'). Пример пароля: '{+}%^[{]{{' */
/*Check: Создать креды и проверить, что буфер с логином добавляется в список буферов. Креды становятся персистентными */
/*Check: Начать создавать креды, пока пароль не засабмитан, скопировать в буфер обмена и только потом сохранить. Убедиться, что правильный пароль вводится */
/*Check: Статус бар обновляется на каждый Ctrl+C. */
/*Check: После удаления всех буферов через пункт меню, по нажатию на Ctrl+Z, они восстанавливаются. */
/*Check: Все пункты меню работают. */
/*Check: Files: /n FolderPath /n FileName0, FileName1
/*Check: Пометить буфер постоянным, пункт меню должен задизэйблиться, и он переместиться вниз. При добавлении новых буферов все постоянные буферы остаются, не удаляются.*/
/*Check: Окно нельзя ресайзить (не перерисовываются кнопки). Попробовать замаксимайзить окно, растянуть - оно не должно поддаваться. */
/*Check: Когда окно программы видно на экране и в это время буфер обновляется, список буферов в окне программы обновляется сразу же. */
/*Check: Запустить студию в режиме админа и попробовать вставить туда текст. */
/*Check: Если копируется несколько файлов, то на бутоне и тултипе будет "<< Files (9) >>: firstFileName ...". Если 1 файл - "<< File >>: firstFileName". */
/*Check: При добавлении нового буфера, тултип на первом буфере не должен всплывать (окно программы должно быть открыто) */
/*Check: Выбрать буфер, нажав на соответствующую кнопку. Окно программы скрывается. */
/*Check: Скопировать несколько клипов. Открыть окно программы слева. Сделать активным другое окно справа (так, чтобы окно программы всё ещё было видимо,
но уже неактивно). Нажать Alt+C. Программа становится активной, без мерцания от перерисовки. */
/*Check: Накопировать максимум буферов. При копировании следующего буфер, он должен добавляться вверх, а самый первый должен удаляться. */
/*Check: Сделать 30 раз принтскрин (чтобы быстро наполнить буфер). Проверить, что все буферы отображаются симметрично, т.е. высота и ширина окна программы выбраны правильно. */
/*Check: Кнопка удалить всё работает */
/*Check: Запустить виндовс, и сразу прилагу, без копирования чего-либо в буфер. ничего не должно быть скопировано. */
/*Check: При изменении текста кнопки буфера, тултипы GotFocus- и MouseOver- тоже изменяются */
/*Check: Изменить второй клип. Затем вставить его кудато. Он должен оказаться вверху, с тултипами и текстом измененными. */
/*Check: Открыть файл, занятый другим приложением. Диалог с ошибкой должен показаться. */